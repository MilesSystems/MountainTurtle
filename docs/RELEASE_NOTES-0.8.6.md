# Mountain Turtle 0.8.6

- Mac setup now reads the current macOS Network Volumes permission state, so Refresh can show an already-enabled Privacy & Security toggle instead of keeping a stale "allow Network Volumes" warning.

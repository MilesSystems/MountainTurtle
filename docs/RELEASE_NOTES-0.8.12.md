# Mountain Turtle 0.8.12

- Fixes writable SFTP deletes in Finder when nested folders contain hidden macOS sidecar files, old AppleDouble files, or other dotfiles that Finder could not see but the remote server still kept.

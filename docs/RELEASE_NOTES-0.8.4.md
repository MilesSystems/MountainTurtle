# Mountain Turtle 0.8.4

- Read-only drives now mount as native macOS read-only volumes as well as rclone-protected remotes, giving Finder a clearer destination read-only failure instead of a misleading source permission alert.
